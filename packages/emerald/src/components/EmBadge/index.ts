export type { EmBadgeProps, EmBadgeVariant } from './EmBadge.vue'

/**
 * Status / count badge (Atom shell).
 *
 * @example
 * ```vue
 * <EmBadge :content="12" variant="primary" />
 * <EmBadge :content="120" :max="99" />
 * <EmBadge dot variant="danger" />
 * ```
 */
export { default as EmBadge } from './EmBadge.vue'
