export type { EmButtonProps, EmButtonSize, EmButtonVariant } from './EmButton.vue'

/**
 * Primary action button. Composes v0 `Button.Root` / `Button.Content` / `Button.Loading`.
 *
 * @example
 * ```vue
 * <EmButton variant="primary" size="md">Save</EmButton>
 * ```
 */
export { default as EmButton } from './EmButton.vue'
