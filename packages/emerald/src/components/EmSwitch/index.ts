export type { EmSwitchProps, EmSwitchSize } from './EmSwitch.vue'

/**
 * Switch control. Composes v0 `Switch.Root` / `Switch.Track` / `Switch.Thumb`.
 *
 * @example
 * ```vue
 * <EmSwitch v-model="enabled">Notifications</EmSwitch>
 * ```
 */
export { default as EmSwitch } from './EmSwitch.vue'
