/**
 * @module locale
 *
 * @remarks
 * Locale adapters for useLocale.
 * Import from `@vuetify/v0/locale` to use.
 *
 * @example
 * ```ts
 * import { Vuetify0LocaleAdapter } from '@vuetify/v0/locale'
 * ```
 */

export * from '#v0/composables/useLocale/adapters'
