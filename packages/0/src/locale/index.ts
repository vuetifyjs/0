/**
 * @module locale
 *
 * @remarks
 * Locale composable for @vuetify/v0.
 * Import adapters from `@vuetify/v0/locale/adapters`.
 *
 * @example
 * ```ts
 * import { createLocalePlugin, useLocale } from '@vuetify/v0/locale'
 * import { Vuetify0LocaleAdapter } from '@vuetify/v0/locale/adapters'
 * ```
 */

export * from '#v0/composables/useLocale'
