/**
 * @module logger/adapters
 *
 * @remarks
 * Logger adapters for useLogger.
 *
 * @example
 * ```ts
 * import { ConsolaLoggerAdapter } from '@vuetify/v0/logger/adapters'
 * ```
 */

export * from '#v0/composables/useLogger/adapters'
