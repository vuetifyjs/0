/**
 * @module rules
 *
 * @remarks
 * Rules composable for @vuetify/v0.
 * Import adapters from `@vuetify/v0/rules/adapters/zod` or `@vuetify/v0/rules/adapters/yup`.
 *
 * @example
 * ```ts
 * import { createRulesPlugin, useRules } from '@vuetify/v0/rules'
 * import { toRule } from '@vuetify/v0/rules/adapters/zod'
 * ```
 */

export * from '#v0/composables/useRules'
