/**
 * @module popover
 *
 * @remarks
 * Popover composable for @vuetify/v0.
 * Import the floating-ui adapter from its specific subpath:
 * - `@vuetify/v0/popover/adapters/floating-ui`
 *
 * @example
 * ```ts
 * import { usePopover, V0PopoverAdapter } from '@vuetify/v0/popover'
 * import { FloatingUIPopoverAdapter } from '@vuetify/v0/popover/adapters/floating-ui'
 * ```
 */

export * from '#v0/composables/usePopover'
