/**
 * @module features/adapters/posthog
 *
 * @remarks
 * PostHog adapter for useFeatures.
 * Requires `posthog-js` peer dependency.
 *
 * @example
 * ```ts
 * import { PostHogFeatureAdapter } from '@vuetify/v0/features/adapters/posthog'
 * ```
 */

export * from '#v0/composables/useFeatures/adapters/posthog'
