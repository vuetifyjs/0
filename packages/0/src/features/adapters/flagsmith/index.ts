/**
 * @module features/adapters/flagsmith
 *
 * @remarks
 * Flagsmith adapter for useFeatures.
 * Requires `flagsmith` peer dependency.
 *
 * @example
 * ```ts
 * import { FlagsmithFeatureAdapter } from '@vuetify/v0/features/adapters/flagsmith'
 * ```
 */

export * from '#v0/composables/useFeatures/adapters/flagsmith'
