---
"@paper/genesis": minor
---

feat(GnDotGrid): add `skew` prop for directional grid stretch — a `-100`–`100` value that stretches the dot grid along one axis while pinching the other by an equal amount, so the pattern reads as pulled in a direction. Defaults to `0` (square cells), so existing usage is unchanged.
