---
"@paper/genesis": minor
---

feat(GnDotGrid): add `skew` prop for perspective grid tilt — a `-100`–`100` value that bends the flat dot grid into a receding surface so the dots bunch toward the far edge, with the sign choosing which edge tips away. Defaults to `0` (flat), so existing usage is unchanged.
