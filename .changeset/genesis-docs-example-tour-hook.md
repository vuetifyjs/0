---
"@paper/genesis": patch
---

chore(GnDocsExample): expose `data-tour` hooks on the docs-example surface — `example-preview` on the preview panel (GnDocsExamplePreview), `example-toggle` on the Show-code toggle, and `example-expand` on the peek Expand pill (GnPeek instance). Stable, docs-consumer-addressable anchors for the v0 docs' guided-tour system to spotlight and drive the example without wrapper components. Purely additive; no visual or behavioral change.
